#pragma once

// Direction represents where the arrow points
enum class Direction { UP,
                       DOWN,
                       LEFT,
                       RIGHT };

enum class ArrowType { Blue,
                       Yellow };
